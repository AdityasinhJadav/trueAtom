{
  "version": 2,
  "buildCommand": "npm ci && npm run setup && npm run build",
  "functions": { "api/index.js": { "runtime": "nodejs20.x" } },
  "routes": [
    { "src": "/build/(.*)", "dest": "/build/client/$1" },
    { "src": "/(.*)", "dest": "/api/index.js" }
  ]
}


